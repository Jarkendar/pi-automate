# pi-automate
Structured automation hub focused on workflow versioning and "Automation-as-Code". Separates core logic (JSON) from environment configuration for clean Git management. A robust, modular engine for scaling personal and professional automations.
